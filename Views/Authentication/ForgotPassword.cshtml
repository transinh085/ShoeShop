@using ShoeShop.ViewModels.Authentication;
@model ForgotPasswordViewModel;
@{
	ViewData["Title"] = "Forgot Password";
}

@section Styles {
	<link href="~/css/account.css" rel="stylesheet">
}

<main class="bg_gray">

	<div class="container margin_60">
		<div class="page_header">
			<h1 class="text-center">Forgot Password</h1>
		</div>
		<!-- /page_header -->
		<div class="row justify-content-center">
			<div class="col-xl-6 col-lg-6 col-md-8">
				<div class="box_account">
					<form class="form_container" asp-controller="Authentication" asp-action="ForgotPassword" method="post" role="form">
						@if (TempData["Error"] != null)
						{
							<div class="col-md-12 alert alert-danger mt-3">
								<span><b>Sorry!</b> - @TempData["Error"]</span>
							</div>
						}
						<div class="form-group">
							<label asp-for="Email" class="form-label"></label>
							<input asp-for="Email" class="form-control mb-1" placeholder="abc@email.com">
							<span asp-validation-for="Email" class="d-block text-danger mt-1"></span>
						</div>
						<div class="text-center"><input type="submit" value="Recover Password" class="btn_1 full-width"></div>
					</form>
					<!-- /form_container -->
				</div>
				<!-- /row -->
			</div>
		</div>
		<!-- /row -->
	</div>
	<!-- /container -->
</main>
@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}